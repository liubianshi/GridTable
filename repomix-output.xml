This file is a merged representation of the entire codebase, combined into a single document by Repomix.
The content has been processed where content has been compressed (code blocks are separated by ⋮---- delimiter).

<file_summary>
This section contains a summary of this file.

<purpose>
This file contains a packed representation of the entire repository's contents.
It is designed to be easily consumable by AI systems for analysis, code review,
or other automated processes.
</purpose>

<file_format>
The content is organized as follows:
1. This summary section
2. Repository information
3. Directory structure
4. Repository files (if enabled)
5. Multiple file entries, each consisting of:
  - File path as an attribute
  - Full contents of the file
</file_format>

<usage_guidelines>
- This file should be treated as read-only. Any changes should be made to the
  original repository files, not this packed version.
- When processing this file, use the file path to distinguish
  between different files in the repository.
- Be aware that this file may contain sensitive information. Handle it with
  the same level of security as you would the original repository.
</usage_guidelines>

<notes>
- Some files may have been excluded based on .gitignore rules and Repomix's configuration
- Binary files are not included in this packed representation. Please refer to the Repository Structure section for a complete list of file paths, including binary files
- Files matching patterns in .gitignore are excluded
- Files matching default ignore patterns are excluded
- Content has been compressed - code blocks are separated by ⋮---- delimiter
- Files are sorted by Git change count (files with more changes are at the bottom)
</notes>

</file_summary>

<directory_structure>
R/
  GridTable.R
tests/
  testthat/
    test-element.R
  testthat.R
.gitignore
DESCRIPTION
NAMESPACE
tags
</directory_structure>

<files>
This section contains the contents of the repository's files.

<file path="tags">
!_TAG_EXTRA_DESCRIPTION	anonymous	/Include tags for non-named objects like lambda/
!_TAG_EXTRA_DESCRIPTION	fileScope	/Include tags of file scope/
!_TAG_EXTRA_DESCRIPTION	pseudo	/Include pseudo tags/
!_TAG_EXTRA_DESCRIPTION	qualified	/Include an extra class-qualified tag entry for each tag/
!_TAG_EXTRA_DESCRIPTION	subparser	/Include tags generated by subparsers/
!_TAG_FIELD_DESCRIPTION	access	/Access (or export) of class members/
!_TAG_FIELD_DESCRIPTION	epoch	/the last modified time of the input file (only for F\/file kind tag)/
!_TAG_FIELD_DESCRIPTION	file	/File-restricted scoping/
!_TAG_FIELD_DESCRIPTION	inherits	/Inheritance information/
!_TAG_FIELD_DESCRIPTION	input	/input file/
!_TAG_FIELD_DESCRIPTION	kind	/[tags output] prepend "kind:" to k\/ (or K\/) field output, [xref and json output] kind in long-name form/
!_TAG_FIELD_DESCRIPTION	line	/Line number of tag definition/
!_TAG_FIELD_DESCRIPTION	name	/tag name/
!_TAG_FIELD_DESCRIPTION	pattern	/pattern/
!_TAG_FIELD_DESCRIPTION	signature	/Signature of routine (e.g. prototype or parameter list)/
!_TAG_FIELD_DESCRIPTION	typeref	/Type and name of a variable or typedef/
!_TAG_FIELD_DESCRIPTION!R	constructor	/function used for making value assigned to the nameattr tag/
!_TAG_FILE_FORMAT	2	/extended format; --format=1 will not append ;" to lines/
!_TAG_FILE_SORTED	1	/0=unsorted, 1=sorted, 2=foldcase/
!_TAG_KIND_DESCRIPTION!R	L,list	/lists explicitly created with `list()'/
!_TAG_KIND_DESCRIPTION!R	c,vector	/vectors explicitly created with `c()'/
!_TAG_KIND_DESCRIPTION!R	d,dataframe	/data frame explicitly created with `data.frame()'/
!_TAG_KIND_DESCRIPTION!R	f,function	/functions/
!_TAG_KIND_DESCRIPTION!R	g,globalVar	/global variables having values other than function()/
!_TAG_KIND_DESCRIPTION!R	l,library	/libraries/
!_TAG_KIND_DESCRIPTION!R	n,nameattr	/names attribtes in vectors, lists, or dataframes/
!_TAG_KIND_DESCRIPTION!R	s,source	/sources/
!_TAG_OUTPUT_EXCMD	mixed	/number, pattern, mixed, or combineV2/
!_TAG_OUTPUT_FILESEP	slash	/slash or backslash/
!_TAG_OUTPUT_MODE	e-ctags	/u-ctags or e-ctags/
!_TAG_OUTPUT_VERSION	0.0	/current.age/
!_TAG_PARSER_VERSION!R	0.0	/current.age/
!_TAG_PATTERN_LENGTH_LIMIT	96	/0 for no limit/
!_TAG_PROC_CWD	/home/liubianshi/Repositories/R-packages/GridTable/	//
!_TAG_PROGRAM_AUTHOR	Universal Ctags Team	//
!_TAG_PROGRAM_NAME	Universal Ctags	/Derived from Exuberant Ctags/
!_TAG_PROGRAM_URL	https://ctags.io/	/official site/
!_TAG_PROGRAM_VERSION	6.1.0	/v6.1.0/
!_TAG_ROLE_DESCRIPTION!R!library	library	/library attached by library function/
!_TAG_ROLE_DESCRIPTION!R!library	require	/library attached by require function/
!_TAG_ROLE_DESCRIPTION!R!source	source	/source loaded by source fucntion/
ALIGN	R/GridTable.R	/^               ALIGN   = ":")$/;"	kind:n	line:6	list:SYMBOL
Cell	R/GridTable.R	/^Cell <- function(tbl, i, j) {$/;"	kind:f	line:41	signature:(tbl, i, j)
Coordinate	R/GridTable.R	/^Coordinate <- function(x, y) {$/;"	kind:f	line:224	signature:(x, y)
Edge	R/GridTable.R	/^Edge <- function(leftnode, rightnode, content, align = NULL) {$/;"	kind:f	line:228	signature:(leftnode, rightnode, content, align = NULL)
FOOTER	R/GridTable.R	/^               FOOTER  = "=",$/;"	kind:n	line:5	list:SYMBOL
GridTable	R/GridTable.R	/^GridTable <- function(data, align = NULL,$/;"	kind:f	line:405	signature:(data, align = NULL, header = NULL, footer = NULL, ...)
HEADER	R/GridTable.R	/^               HEADER  = "=",$/;"	kind:n	line:4	list:SYMBOL
LINE	R/GridTable.R	/^               LINE    = "-",$/;"	kind:n	line:3	list:SYMBOL
MERGED_CELL_OPTION	R/GridTable.R	/^MERGED_CELL_OPTION <- list(drop_content = FALSE,$/;"	kind:L	line:8
Node	R/GridTable.R	/^Node <- function(coordinate, symbol = "+") {$/;"	kind:f	line:594	signature:(coordinate, symbol = "+")
Row	R/GridTable.R	/^Row <- function(..., row_no = NULL) {$/;"	kind:f	line:729	signature:(..., row_no = NULL)
SIDE	R/GridTable.R	/^               SIDE    = "|",$/;"	kind:n	line:2	list:SYMBOL
SYMBOL	R/GridTable.R	/^SYMBOL <- list(VERTICE = "+",$/;"	kind:L	line:1
T	tests/testthat/test-element.R	/^T <- Table(e1, e2, e3)$/;"	kind:g	line:14
Table	R/GridTable.R	/^Table <- function(...) {$/;"	kind:f	line:840	signature:(...)
VERTICE	R/GridTable.R	/^SYMBOL <- list(VERTICE = "+",$/;"	kind:n	line:1	list:SYMBOL
`<.Edge`	R/GridTable.R	/^`<.Edge` <- function(e1, e2) {$/;"	kind:f	line:30	signature:(e1, e2)
`<.Node`	R/GridTable.R	/^`<.Node` <- function(n1, n2) {$/;"	kind:f	line:12	signature:(n1, n2)
`==.Edge`	R/GridTable.R	/^`==.Edge` <- function(e1, e2) {$/;"	kind:f	line:26	signature:(e1, e2)
`==.Node`	R/GridTable.R	/^`==.Node` <- function(n1, n2) {$/;"	kind:f	line:22	signature:(n1, n2)
`>.Node`	R/GridTable.R	/^`>.Node` <- function(n1, n2) {$/;"	kind:f	line:17	signature:(n1, n2)
align	R/GridTable.R	/^                   align     = align,$/;"	kind:n	line:253	list:Edge.anonListdbd18ed40e07
anonFuncdbd18ed40500	R/GridTable.R	/^    keep <- purrr::map_lgl(seq_along(sep_p), function(x) {$/;"	kind:f	line:179	function:column_start_end_points	signature:(x)
anonFuncdbd18ed40600	R/GridTable.R	/^    start_end_points <- unlist(purrr::map(seq_along(sep_p), function(i) {$/;"	kind:f	line:189	function:column_start_end_points	signature:(i)
anonFuncdbd18ed40a00	R/GridTable.R	/^               function(i) start_end_points[2 * i - 1:0])$/;"	kind:f	line:211	function:column_start_end_points	signature:(i)
anonListdbd18ed40307	R/GridTable.R	/^    list(col = col, row = row)$/;"	kind:L	line:124	function:cell_position_info
anonListdbd18ed40e07	R/GridTable.R	/^    structure(list(leftnode  = leftnode,$/;"	kind:L	line:250	function:Edge
anonListdbd18ed41f07	R/GridTable.R	/^        return(structure(list(nodes = NULL,$/;"	kind:L	line:338	function:empty_row
anonListdbd18ed42907	R/GridTable.R	/^    structure(list(coordinate = coordinate,$/;"	kind:L	line:596	function:Node
anonListdbd18ed42b07	R/GridTable.R	/^    structure(list(nodes = nodes,$/;"	kind:L	line:752	function:Row
anonListdbd18ed42d07	R/GridTable.R	/^    structure(list(rows = rows, length = length, width = width),$/;"	kind:L	line:855	function:Table
anonListdbd18ed43707	R/GridTable.R	/^        purrr::map(list(rows = rows, cols = cols), \\(x) {$/;"	kind:L	line:956	function:valid_merged_cell
anonVecbb34503e0106	tests/testthat/test-element.R	/^    merge_cells(c(1,3), 1, wrap = TRUE) |>$/;"	kind:c	line:31
anonVecbb34503e0206	tests/testthat/test-element.R	/^    merge_cells(c(1,2), c(2,4), wrap = TRUE) |>$/;"	kind:c	line:32
anonVecbb34503e0306	tests/testthat/test-element.R	/^    merge_cells(c(1,2), c(2,4), wrap = TRUE) |>$/;"	kind:c	line:32	vector:anonVecbb34503e0206
anonVecbb34503e0406	tests/testthat/test-element.R	/^    merge_cells(c(4,5), 2, drop_content = TRUE, middle = TRUE)$/;"	kind:c	line:33
anonVecdbd18ed40c06	R/GridTable.R	/^    structure(c(toInteger(x), toInteger(y)), class = "Coordinate")$/;"	kind:c	line:225	function:Coordinate
anonVecdbd18ed42706	R/GridTable.R	/^        do.call(Row, c(integrate_edge_list(edges), row_no = i))$/;"	kind:c	line:584	function:merge_cell_list
args	R/GridTable.R	/^    args <- list(...)$/;"	kind:L	line:408	function:GridTable
args	R/GridTable.R	/^    args <- list(...)$/;"	kind:L	line:511	function:kable_to_grid
args	R/GridTable.R	/^    args <- list(...)$/;"	kind:L	line:724	function:row_no_allequal
args	R/GridTable.R	/^    args <- list(...)$/;"	kind:L	line:894	function:toString.GridTable
attrs	R/GridTable.R	/^    attrs <- list(...)$/;"	kind:L	line:328	function:edge_update
cal_column_width	R/GridTable.R	/^cal_column_width <- function(x) {$/;"	kind:f	line:34	signature:(x)
cell_content	R/GridTable.R	/^cell_content <- function(tbl, i = NULL, j = NULL, info = NULL) {$/;"	kind:f	line:127	signature:(tbl, i = NULL, j = NULL, info = NULL)
cell_merge_info	R/GridTable.R	/^cell_merge_info <- function(tbl, i, j) {$/;"	kind:f	line:79	signature:(tbl, i, j)
cell_position_info	R/GridTable.R	/^cell_position_info <- function(tbl, i, j) {$/;"	kind:f	line:101	signature:(tbl, i, j)
class	R/GridTable.R	/^                   row_no = row_no(edges[[1]])), class = "Row")$/;"	kind:n	line:755	list:Row.anonListdbd18ed42b07
class	R/GridTable.R	/^    structure(c(toInteger(x), toInteger(y)), class = "Coordinate")$/;"	kind:n	line:225	vector:Coordinate.anonVecdbd18ed40c06
col	R/GridTable.R	/^    col <- list(start = if (j[1] == 1) 1 else get_col_nums(1:(j[1] - 1)),$/;"	kind:L	line:106	function:cell_position_info
col	R/GridTable.R	/^    list(col = col, row = row)$/;"	kind:n	line:124	list:cell_position_info.anonListdbd18ed40307
col_no	R/GridTable.R	/^col_no <- function(x) {$/;"	kind:f	line:213	signature:(x)
cols	R/GridTable.R	/^        purrr::map(list(rows = rows, cols = cols), \\(x) {$/;"	kind:n	line:956	list:valid_merged_cell.anonListdbd18ed43707
column_start_end_points	R/GridTable.R	/^column_start_end_points <-  function (line, sep = " ") {$/;"	kind:f	line:174	signature:(line, sep = " ")
content	R/GridTable.R	/^                   content   = content,$/;"	kind:n	line:252	list:Edge.anonListdbd18ed40e07
coordinate	R/GridTable.R	/^    structure(list(coordinate = coordinate,$/;"	kind:n	line:596	list:Node.anonListdbd18ed42907
drop_content	R/GridTable.R	/^MERGED_CELL_OPTION <- list(drop_content = FALSE,$/;"	kind:n	line:8	list:MERGED_CELL_OPTION
drop_content	tests/testthat/test-element.R	/^    merge_cells(c(4,5), 2, drop_content = TRUE, middle = TRUE)$/;"	kind:n	line:33	vector:anonVecbb34503e0406
dt	tests/testthat/test-element.R	/^dt <- data.table::fread("$/;"	kind:g	line:22
e1	tests/testthat/test-element.R	/^e1  <- Row(Edge(r11, r12, "-", "l"))$/;"	kind:g	line:11
e11	tests/testthat/test-element.R	/^e11 <- Edge(r11, r12, "-")$/;"	kind:g	line:16
e12	tests/testthat/test-element.R	/^e12 <- Edge(r11, r13, "-")$/;"	kind:g	line:17
e2	tests/testthat/test-element.R	/^e2  <- Row(Edge(r21, r22, "test"))$/;"	kind:g	line:12
e3	tests/testthat/test-element.R	/^e3  <- Row(Edge(r31, r32, "-"))$/;"	kind:g	line:13
edge_left_extend	R/GridTable.R	/^edge_left_extend <- function(e1, e2) {$/;"	kind:f	line:259	signature:(e1, e2)
edge_merge	R/GridTable.R	/^edge_merge <- function(e1, e2) {$/;"	kind:f	line:265	signature:(e1, e2)
edge_relation	R/GridTable.R	/^edge_relation <- function(e1, e2) {$/;"	kind:f	line:322	signature:(e1, e2)
edge_update	R/GridTable.R	/^edge_update <- function(e, ...) {$/;"	kind:f	line:327	signature:(e, ...)
edges	R/GridTable.R	/^                              edges = NULL,$/;"	kind:n	line:339	list:empty_row.anonListdbd18ed41f07
edges	R/GridTable.R	/^                   edges = edges,$/;"	kind:n	line:753	list:Row.anonListdbd18ed42b07
edges	R/GridTable.R	/^    edges <- list(...)$/;"	kind:L	line:730	function:Row
empty_row	R/GridTable.R	/^empty_row <- function(row_no, left_col_no = NULL, right_col_no = NULL) {$/;"	kind:f	line:335	signature:(row_no, left_col_no = NULL, right_col_no = NULL)
end	R/GridTable.R	/^                end   = get_col_nums(1:j[length(j)]))$/;"	kind:n	line:108	list:cell_position_info.col
end	R/GridTable.R	/^                end   = get_row_nums(1:i[length(i)]))$/;"	kind:n	line:111	list:cell_position_info.row
format_column	R/GridTable.R	/^format_column <- function(x, digits = 3L, ...) {$/;"	kind:f	line:349	signature:(x, digits = 3L, ...)
format_one_num	R/GridTable.R	/^format_one_num <- function(z, digits, nsmall = 3L,$/;"	kind:f	line:366	signature:(z, digits, nsmall = 3L, width = NULL, na.replace = "", big.mark = ",")
gdt	tests/testthat/test-element.R	/^gdt <- GridTable(dt, header = 3, align = "lrrr") |>$/;"	kind:g	line:30
get	R/GridTable.R	/^    get <- function(x, i) if (is.atomic(x))        x[i]     else x[[i]]$/;"	kind:f	line:472	function:is_overlaped	signature:(x, i)
get_cells_from	R/GridTable.R	/^get_cells_from <- function(gtable) {$/;"	kind:f	line:393	signature:(gtable)
get_col_nums	R/GridTable.R	/^    get_col_nums <- function(j) sum(width[j] + 3) + 1$/;"	kind:f	line:105	function:cell_position_info	signature:(j)
get_row_nums	R/GridTable.R	/^    get_row_nums <- function(i) sum(height[i] + 1) + 1$/;"	kind:f	line:104	function:cell_position_info	signature:(i)
height_of	R/GridTable.R	/^height_of <- function(df, base = NULL) {$/;"	kind:f	line:432	signature:(df, base = NULL)
ifthen	R/GridTable.R	/^ifthen <- function(x, y) {$/;"	kind:f	line:444	signature:(x, y)
integrate_edge_list	R/GridTable.R	/^integrate_edge_list <- function(edge_list) {$/;"	kind:f	line:448	signature:(edge_list)
is_overlaped	R/GridTable.R	/^is_overlaped <- function(x, y) {$/;"	kind:f	line:470	signature:(x, y)
kable_to_grid	R/GridTable.R	/^kable_to_grid <- function(kbl, ...) {$/;"	kind:f	line:480	signature:(kbl, ...)
last_node	R/GridTable.R	/^last_node <- function(row) {$/;"	kind:f	line:518	signature:(row)
leftnode	R/GridTable.R	/^    structure(list(leftnode  = leftnode,$/;"	kind:n	line:250	list:Edge.anonListdbd18ed40e07
length	R/GridTable.R	/^    structure(list(rows = rows, length = length, width = width),$/;"	kind:n	line:855	list:Table.anonListdbd18ed42d07
max	R/GridTable.R	/^    max <- function(z)    if (get(z,1) < get(z,2)) get(z,2) else get(z,1)$/;"	kind:f	line:474	function:is_overlaped	signature:(z)
merge_cell_list	R/GridTable.R	/^merge_cell_list <- function(cells) {$/;"	kind:f	line:572	signature:(cells)
merge_cells	R/GridTable.R	/^merge_cells <- function(tbl, i = NULL, j = NULL, cancel = NULL, ...) {$/;"	kind:f	line:524	signature:(tbl, i = NULL, j = NULL, cancel = NULL, ...)
merged_cell	R/GridTable.R	/^    merged_cell <- c(valid_merged_cell(i, j, tbl), list(...))$/;"	kind:c	line:559	function:merge_cells
middle	R/GridTable.R	/^                           middle       = FALSE,$/;"	kind:n	line:9	list:MERGED_CELL_OPTION
middle	tests/testthat/test-element.R	/^    merge_cells(c(4,5), 2, drop_content = TRUE, middle = TRUE)$/;"	kind:n	line:33	vector:anonVecbb34503e0406
min	R/GridTable.R	/^    min <- function(z)    if (get(z,1) < get(z,2)) get(z,1) else get(z,2)$/;"	kind:f	line:473	function:is_overlaped	signature:(z)
minmax	R/GridTable.R	/^minmax <- function(x, na.rm = TRUE) {$/;"	kind:f	line:589	signature:(x, na.rm = TRUE)
n	R/GridTable.R	/^                              n = 0,$/;"	kind:n	line:340	list:empty_row.anonListdbd18ed41f07
n	R/GridTable.R	/^                   n = length(nodes),$/;"	kind:n	line:754	list:Row.anonListdbd18ed42b07
new	R/GridTable.R	/^    new <- list(shift(old))$/;"	kind:L	line:452	function:integrate_edge_list
newattrs	R/GridTable.R	/^    newattrs <- c(old_merged_cell, list(merged_cell))$/;"	kind:c	line:566	function:merge_cells
node_list_merge	R/GridTable.R	/^node_list_merge <- function(nodes1, nodes2) {$/;"	kind:f	line:610	signature:(nodes1, nodes2)
node_merge	R/GridTable.R	/^node_merge <- function(n1, n2) {$/;"	kind:f	line:602	signature:(n1, n2)
nodes	R/GridTable.R	/^        return(structure(list(nodes = NULL,$/;"	kind:n	line:338	list:empty_row.anonListdbd18ed41f07
nodes	R/GridTable.R	/^    structure(list(nodes = nodes,$/;"	kind:n	line:752	list:Row.anonListdbd18ed42b07
num	R/GridTable.R	/^                num   = get_col_nums(j),$/;"	kind:n	line:107	list:cell_position_info.col
num	R/GridTable.R	/^                num   = get_row_nums(i),$/;"	kind:n	line:110	list:cell_position_info.row
out	tests/testthat/test-element.R	/^out <- file.path(Sys.getenv("NUTSTORE"),$/;"	kind:g	line:40
pandoc_wrap	R/GridTable.R	/^pandoc_wrap <- function(x) {$/;"	kind:f	line:621	signature:(x)
parse_number_adjust	R/GridTable.R	/^parse_number_adjust <- function(num, x) {$/;"	kind:f	line:633	signature:(num, x)
print.GridTable	R/GridTable.R	/^print.GridTable <- function(gtable, drop_empty_line = TRUE, ...) {$/;"	kind:f	line:673	signature:(gtable, drop_empty_line = TRUE, ...)
r11	tests/testthat/test-element.R	/^r11 <- Node(Coordinate(1L, 1L), "+")$/;"	kind:g	line:4
r12	tests/testthat/test-element.R	/^r12 <- Node(Coordinate(1L, 9L), "+")$/;"	kind:g	line:5
r13	tests/testthat/test-element.R	/^r13 <- Node(Coordinate(1L, 12L), "+")$/;"	kind:g	line:6
r21	tests/testthat/test-element.R	/^r21 <- Node(Coordinate(2L, 1L), "|")$/;"	kind:g	line:7
r22	tests/testthat/test-element.R	/^r22 <- Node(Coordinate(2L, 9L), "|")$/;"	kind:g	line:8
r31	tests/testthat/test-element.R	/^r31 <- Node(Coordinate(3L, 1L), "+")$/;"	kind:g	line:9
r32	tests/testthat/test-element.R	/^r32 <- Node(Coordinate(3L, 9L), "+")$/;"	kind:g	line:10
range_relation	R/GridTable.R	/^range_relation <- function(s1, s2, na.rm = TRUE) {$/;"	kind:f	line:690	signature:(s1, s2, na.rm = TRUE)
re	tests/testthat/test-element.R	/^re <- kable_to_grid(out$out)$/;"	kind:g	line:45
rightnode	R/GridTable.R	/^                   rightnode = rightnode,$/;"	kind:n	line:251	list:Edge.anonListdbd18ed40e07
row	R/GridTable.R	/^    list(col = col, row = row)$/;"	kind:n	line:124	list:cell_position_info.anonListdbd18ed40307
row	R/GridTable.R	/^    row <- list(start = if (i[1] == 1) 1 else get_row_nums(1:(i[1] - 1)),$/;"	kind:L	line:109	function:cell_position_info
row_no	R/GridTable.R	/^                              row_no = row_no),$/;"	kind:n	line:341	list:empty_row.anonListdbd18ed41f07
row_no	R/GridTable.R	/^                   row_no = row_no(edges[[1]])), class = "Row")$/;"	kind:n	line:755	list:Row.anonListdbd18ed42b07
row_no	R/GridTable.R	/^        do.call(Row, c(integrate_edge_list(edges), row_no = i))$/;"	kind:n	line:584	vector:merge_cell_list.anonVecdbd18ed42706
row_no	R/GridTable.R	/^row_no <- function(x) {$/;"	kind:f	line:680	signature:(x)
row_no_allequal	R/GridTable.R	/^row_no_allequal <- function(...) {$/;"	kind:f	line:723	signature:(...)
rows	R/GridTable.R	/^        purrr::map(list(rows = rows, cols = cols), \\(x) {$/;"	kind:n	line:956	list:valid_merged_cell.anonListdbd18ed43707
rows	R/GridTable.R	/^    rows    <- list(...)$/;"	kind:L	line:841	function:Table
rows	R/GridTable.R	/^    structure(list(rows = rows, length = length, width = width),$/;"	kind:n	line:855	list:Table.anonListdbd18ed42d07
sep	tests/testthat/test-element.R	/^", sep = "|")$/;"	kind:g	line:28
set_attr	R/GridTable.R	/^set_attr <- function(tbl, attr = NULL, value = NULL, ...) {$/;"	kind:f	line:760	signature:(tbl, attr = NULL, value = NULL, ...)
shift	R/GridTable.R	/^shift <- function (x, drop = TRUE) {$/;"	kind:f	line:783	signature:(x, drop = TRUE)
sort_edge_list	R/GridTable.R	/^sort_edge_list <- function(edge_list) {$/;"	kind:f	line:797	signature:(edge_list)
start	R/GridTable.R	/^    col <- list(start = if (j[1] == 1) 1 else get_col_nums(1:(j[1] - 1)),$/;"	kind:n	line:106	list:cell_position_info.col
start	R/GridTable.R	/^    row <- list(start = if (i[1] == 1) 1 else get_row_nums(1:(i[1] - 1)),$/;"	kind:n	line:109	list:cell_position_info.row
str_match	R/GridTable.R	/^str_match <- function(x, pattern) regmatches(x, regexec(pattern, x))$/;"	kind:f	line:806	signature:(x, pattern)
str_width	R/GridTable.R	/^str_width <- function(x) nchar(x, type = "width")$/;"	kind:f	line:805	signature:(x)
substr_width	R/GridTable.R	/^substr_width <- function(x, start, end) {$/;"	kind:f	line:808	signature:(x, start, end)
symbol	R/GridTable.R	/^                   symbol    = symbol),$/;"	kind:n	line:255	list:Edge.anonListdbd18ed40e07
symbol	R/GridTable.R	/^                   symbol = symbol),$/;"	kind:n	line:597	list:Node.anonListdbd18ed42907
temp	R/GridTable.R	/^    temp <- list(shift(old))$/;"	kind:L	line:453	function:integrate_edge_list
toInteger	R/GridTable.R	/^toInteger <- function(x) {$/;"	kind:f	line:835	signature:(x)
toString.Edge	R/GridTable.R	/^toString.Edge <- function(edge) {$/;"	kind:f	line:860	signature:(edge)
toString.GridTable	R/GridTable.R	/^toString.GridTable <- function(gtable, ...) {$/;"	kind:f	line:891	signature:(gtable, ...)
toString.Row	R/GridTable.R	/^toString.Row <- function(row) {$/;"	kind:f	line:886	signature:(row)
toString.Table	R/GridTable.R	/^toString.Table <- function(tbl, drop_empty_line = TRUE, ...) {$/;"	kind:f	line:920	signature:(tbl, drop_empty_line = TRUE, ...)
type	R/GridTable.R	/^                   type      = type,$/;"	kind:n	line:254	list:Edge.anonListdbd18ed40e07
valid_align	R/GridTable.R	/^valid_align <- function(data, align = NULL) {$/;"	kind:f	line:929	signature:(data, align = NULL)
valid_merged_cell	R/GridTable.R	/^valid_merged_cell <- function(rows, cols, gridtable) {$/;"	kind:f	line:951	signature:(rows, cols, gridtable)
width	R/GridTable.R	/^    structure(list(rows = rows, length = length, width = width),$/;"	kind:n	line:855	list:Table.anonListdbd18ed42d07
wrap	R/GridTable.R	/^                           wrap         = FALSE)$/;"	kind:n	line:10	list:MERGED_CELL_OPTION
wrap	tests/testthat/test-element.R	/^    merge_cells(c(1,2), c(2,4), wrap = TRUE) |>$/;"	kind:n	line:32	vector:anonVecbb34503e0206.anonVecbb34503e0306
wrap	tests/testthat/test-element.R	/^    merge_cells(c(1,3), 1, wrap = TRUE) |>$/;"	kind:n	line:31	vector:anonVecbb34503e0106
</file>

<file path="tests/testthat.R">
library(testthat)
library(gridtable)

test_check("gridtable")
</file>

<file path=".gitignore">
.Rproj.user
.Rhistory
.RData
.Ruserdata
src/*.o
src/*.so
src/*.dll
src-i386/*
Rplots.pdf
.Rhistor*
/check
.Renviron
**/desktop.ini
</file>

<file path="NAMESPACE">
# Generated by roxygen2: do not edit by hand

S3method(print,GridTable)
S3method(toString,GridTable)
export(GridTable)
export(kable_to_grid)
export(merge_cells)
export(row_no)
export(set_attr)
</file>

<file path="DESCRIPTION">
Package: GridTable
Title: What the Package Does (One Line, Title Case)
Version: 0.0.0.9002
Authors@R:
    person("First", "Last", , "first.last@example.com", role = c("aut", "cre"),
           comment = c(ORCID = "YOUR-ORCID-ID"))
Description: What the package does (one paragraph).
License: `use_mit_license()`, `use_gpl3_license()` or friends to pick a
    license
Encoding: UTF-8
Roxygen: list(markdown = TRUE)
RoxygenNote: 7.2.3
Depends:
    R (>= 4.0.0),
    purrr,
    data.table
</file>

<file path="tests/testthat/test-element.R">
context("Test element")


r11 <- Node(Coordinate(1L, 1L), "+")
r12 <- Node(Coordinate(1L, 9L), "+")
r13 <- Node(Coordinate(1L, 12L), "+")
r21 <- Node(Coordinate(2L, 1L), "|")
r22 <- Node(Coordinate(2L, 9L), "|")
r31 <- Node(Coordinate(3L, 1L), "+")
r32 <- Node(Coordinate(3L, 9L), "+")
e1  <- Row(Edge(r11, r12, "-", "l"))
e2  <- Row(Edge(r21, r22, "test"))
e3  <- Row(Edge(r31, r32, "-"))
T <- Table(e1, e2, e3)

e11 <- Edge(r11, r12, "-")
e12 <- Edge(r11, r13, "-")
edge_relation(e11, e12)
edge_merge(e11, e12)


dt <- data.table::fread("
Location            | Temperature 1961-1990 ||
                    | in degree Celsius     ||
                    | min   | mean  | max   
Antarctica          | -89.2 | N/A   | 19.8  
Earth               | -89.2 | 14    | 56.7  
", sep = "|")

gdt <- GridTable(dt, header = 3, align = "lrrr") |>
    merge_cells(c(1,3), 1, wrap = TRUE) |>
    merge_cells(c(1,2), c(2,4), wrap = TRUE) |>
    merge_cells(c(4,5), 2, drop_content = TRUE, middle = TRUE)
set_attr(gdt, "width", "2/2")
set_attr(gdt, "height", "2+1")
set_attr(gdt, "width", "b-3")
print(gdt, drop_empty_line = FALSE)
print(gdt)

out <- file.path(Sys.getenv("NUTSTORE"),
                            "论文/IIA 和国际直接投资/写作/iia_ma",
                            "基本模型回归结果.Rds") |>
        readRDS()

re <- kable_to_grid(out$out)
set_attr(re, "header", 2)
set_attr(re, header = 2)
set_attr(re, width = "B/2")
merge_cells(re, 1, 2:7)
# system.time(print(re))
</file>

<file path="R/GridTable.R">
SYMBOL <- list(VERTICE = "+",
               SIDE    = "|",
               LINE    = "-",
               HEADER  = "=",
               FOOTER  = "=",
               ALIGN   = ":")

MERGED_CELL_OPTION <- list(drop_content = FALSE,
                           middle       = FALSE,
                           wrap         = FALSE)

`<.Node` <- function(n1, n2) {
    stopifnot(row_no(n1) == row_no(n2))
    col_no(n1) < col_no(n2)
}

`>.Node` <- function(n1, n2) {
    stopifnot(row_no(n1) == row_no(n2))
    col_no(n1) > col_no(n2)
}

`==.Node` <- function(n1, n2) {
    all(n1$coordinate == n2$coordinate)
}

`==.Edge` <- function(e1, e2) {
    e1$leftnode == e2$leftnode && e1$rightnode == e2$rightnode
}

`<.Edge` <- function(e1, e2) {
    e1$leftnode < e2$leftnode
}

cal_column_width <- function(x) {
    x <- x[!is.na(x)]
    trimws(x) |>
    str_width() |>
    max()
}

Cell <- function(tbl, i, j) {
    info <- cell_merge_info(tbl, i, j)
    if (isTRUE(info$merged) && isFALSE(info$first_cell)) {
        return(NULL)
    }
    info <- c(info, cell_position_info(tbl, info$i, info$j))
    info$align <- attr(tbl, "align")[info$j[1]]
    info$content <- cell_content(tbl, info = info)


    edges <- purrr::map(info$row$start:info$row$end, \(rowno) {
        node_symbol  <- if      (rowno == info$row$start) SYMBOL$VERTICE
                        else if (rowno == info$row$end)   SYMBOL$VERTICE
                        else                              SYMBOL$SIDE

        leftnode     <- Node(Coordinate(rowno, info$col$start), node_symbol)
        rightnode    <- Node(Coordinate(rowno, info$col$end),   node_symbol)

        edge_content <-
            if (rowno == info$row$start) {
                if      (info$row$isHeaderLine$start) SYMBOL$HEADER
                else if (info$row$isFooterLine$start) SYMBOL$FOOTER
                else                                  SYMBOL$LINE
            } else if (rowno == info$row$end) {
                if      (info$row$isHeaderLine$end)   SYMBOL$HEADER
                else if (info$row$isFooterLine$end)   SYMBOL$FOOTER
                else                                  SYMBOL$LINE
            }
            else {
                purrr::pluck(info$content, rowno - info$row$start, .default = "")
            }

        Edge(leftnode, rightnode, edge_content, info$align)
    })

    structure(edges, start = info$row$start, end = info$row$end, class = "Cell")
}

cell_merge_info <- function(tbl, i, j) {
    stopifnot(inherits(tbl, "GridTable"))
    stopifnot(length(i) == 1 && length(j) == 1)

    meta <- MERGED_CELL_OPTION
    meta$merged <- FALSE
    meta$i <- toInteger(i)
    meta$j <- toInteger(j)

    for (m in attr(tbl, "merged_cells")) {
        if (meta$i %in% m$rows[1]:m$rows[2] && meta$j %in% m$cols[1]:m$cols[2]) {
            meta$merged <- TRUE
            meta$first_cell <- (meta$i == m$rows[1] && meta$j == m$cols[1])
            purrr::iwalk(m, \(x, y) if (y %in% names(meta)) meta[[y]] <<- x)
            meta$i <- m$rows[1]:m$rows[2]
            meta$j <- m$cols[1]:m$cols[2]
            break
        }
    }
    meta
}

cell_position_info <- function(tbl, i, j) {
    height <- attr(tbl, "height")
    width  <- attr(tbl, "width")
    get_row_nums <- function(i) sum(height[i] + 1) + 1
    get_col_nums <- function(j) sum(width[j] + 3) + 1
    col <- list(start = if (j[1] == 1) 1 else get_col_nums(1:(j[1] - 1)),
                num   = get_col_nums(j),
                end   = get_col_nums(1:j[length(j)]))
    row <- list(start = if (i[1] == 1) 1 else get_row_nums(1:(i[1] - 1)),
                num   = get_row_nums(i),
                end   = get_row_nums(1:i[length(i)]))

    table_header <- attr(tbl, "header")
    row$isHeaderLine$start = FALSE
    row$isHeaderLine$end   = row$end == sum(height[1:table_header] + 1) + 1

    table_footer_rows <- purrr::map(attr(tbl, "footer"), \(f) {
        if (is.null(f) || is.infinite(f) || f <= 0) return(c(-1, -1))
        c(sum(height[1:(f-1)] + 1) + 1, sum(height + 1) + 1)
    })
    row$isFooterLine$start <- row$start %in% purrr::map_int(table_footer_rows, 1)
    row$isFooterLine$end   <- row$end %in% purrr::map_int(table_footer_rows, 2)

    list(col = col, row = row)
}

cell_content <- function(tbl, i = NULL, j = NULL, info = NULL) {
    if ((is.null(i) || is.null(j)) && is.null(info)) {
        stop("Need set i and j or set info", call. = FALSE)
    }
    if (is.null(i) || is.null(j)) {
        i <- info$i
        j <- info$j
    }
    if (is.null(info)) {
        info <- c(cell_merge_info(tbl, i, j), cell_position_info(tbl, i, j))
    }
    content <- if (isTRUE(info$drop_content)) {
        tbl[i[1], j[1]]
    } else {
        subm <- as.matrix(tbl)[i, j]
        dim(subm) <- c(length(i), length(j))
        apply(subm, 2, paste, collapse = "\n")
    }

    content <- unlist(strsplit(as.character(content), "\n"))
    content <- content[!grepl("^\\s*(&nbsp;)?\\s*$", content, perl = TRUE)]
    if (length(content) == 0) return("")

    if (length(content) > info$row$num - 2) {
        set_attr(tbl, height = paste0(i[1], "+", length(content) - info$row$num + 2))
        stop("Adjust the height", call. = FALSE)
    }
    if (isTRUE(info$wrap) && length(content) > 1) {
        content[-length(content)] <- pandoc_wrap(content[-length(content)])
    }
    if (isTRUE(info$middle)) {
        content <- c(rep("", (info$row$num - 2 - length(content)) / 2), content)
    }

    max_content_width <- max(purrr::map_int(content, str_width))
    if (max_content_width + 4 > info$col$num) {
        shorter        <- max_content_width + 4 - info$col$num
        inc            <- ceiling(shorter / length(j))
        table_width    <- attr(tbl, "width")
        table_width[j] <- table_width[j] + inc
        data.table::setattr(tbl, "width", table_width)
        stop("Adjust the width", call. = FALSE)
    }

    content
}

column_start_end_points <-  function (line, sep = " ") {
    line_width <- str_width(line)
    sep_p <- which(strsplit(line, "")[[1]] == sep)
    if (length(sep_p) == 0)
        return(NULL)
    keep <- purrr::map_lgl(seq_along(sep_p), function(x) {
        if (x == 1)
            return(TRUE)
        if (x == length(sep_p))
            return(TRUE)
        if (sep_p[x] - sep_p[x - 1] == 1 && sep_p[x + 1] - sep_p[x] == 1)
            return(FALSE)
        return(TRUE)
    })
    sep_p <- sep_p[keep]
    start_end_points <- unlist(purrr::map(seq_along(sep_p), function(i) {
        if (i == 1) {
            return(if (all(sep_p[1:2] == 1:2)) NULL else if (sep_p[1] ==
                1) 2 else c(1, sep_p[1] - 1))
        }
        if (i == length(sep_p)) {
            if (all(sep_p[length(sep_p) - 1:0] == line_width -
                1:0)) {
                return(NULL)
            }
            if (sep_p[length(sep_p)] == line_width) {
                return(line_width - 1)
            }
            return(c(sep_p[length(sep_p)] + 1, line_width))
        }
        if (sep_p[i] == sep_p[i - 1] + 1)
            return(sep_p[i] + 1)
        if (sep_p[i] == sep_p[i + 1] - 1)
            return(sep_p[i] - 1)
        return(c(sep_p[i] - 1, sep_p[i] + 1))
    }))
    purrr::map(seq_len(length(start_end_points)/2),
               function(i) start_end_points[2 * i - 1:0])
}
col_no <- function(x) {
    switch(class(x),
        Coordinate = x[2],
        Node       = col_no(x$coordinate),
        Edge       = c(col_no(x$leftnode), col_no(x$rightnode)),
        list       = purrr::map_int(x, col_no),
        default    = stop("wrong object")
    )
}


Coordinate <- function(x, y) {
    structure(c(toInteger(x), toInteger(y)), class = "Coordinate")
}

Edge <- function(leftnode, rightnode, content, align = NULL) {
    stopifnot(class(leftnode)  == "Node" &&
              class(rightnode) == "Node" &&
              is.character(content) &&
              length(content) == 1 &&
              (is.null(align) || isTRUE(align %in% c("l", "r", "c"))) &&
              row_no(leftnode) == row_no(rightnode) &&
              col_no(leftnode) <  col_no(rightnode))
    type <- "Normal"
    symbol <- NULL
    if (leftnode$symbol != SYMBOL$SIDE && rightnode$symbol != SYMBOL$SIDE) {
        symbol <- local({
            cont <- trimws(content)
            unique(strsplit(cont, "")[[1]])
        })
        stopifnot(str_width(symbol) == 1)
        type <- unlist(purrr::imap(SYMBOL, ~ if (.x == symbol) .y else NULL))
    } else if (!grepl("\\S", content, perl = TRUE)) {
        type <- "Empty"
        symbol <- " "
    }

    structure(list(leftnode  = leftnode,
                   rightnode = rightnode,
                   content   = content,
                   align     = align,
                   type      = type,
                   symbol    = symbol),
              class = "Edge")
}

edge_left_extend <- function(e1, e2) {
    updated_node <- node_merge(e1$rightnode, e2$leftnode)
    return(list(edge_update(e1, rightnode = updated_node),
                edge_update(e2, leftnode = updated_node)))
}

edge_merge <- function(e1, e2) {
    if (col_no(e1$leftnode) > col_no(e2$leftnode)) return(edge_merge(e2, e1))
    switch(edge_relation(e1, e2),
        LEFT = {
            list(e1, Edge(e1$rightnode, e2$leftnode, ""), e2)
        },
        ADJACENT_LEFT = {
            edge_left_extend(e1, e2)
        },
        CONTAIN_SAME_END = {
            left_part    <- edge_update(e1, rightnode = e2$leftnode)
            overlap_part <- edge_update(e1, leftnode = e2$leftnode, align = e2$align)
            c(list(excess_part), edge_merge(overlap_part, e2))
        },
        OVERLAP_LEFT = {
            left_part    <- edge_update(e1, rightnode = e2$leftnode)
            overlap_p1   <- edge_update(e1, leftnode  = e2$leftnode)
            overlap_p2   <- edge_update(e2, rightnode = e1$rightnode)
            right_part   <- edge_update(e2, leftnode  = e1$rightnode)
            c(list(excess_part), edge_merge(overlap_p1, eoverlap_p2),
              list(right_part))
        },
        IN_SAME_START = {
            overlap_part <- edge_update(e2, rightnode = e1$rightnode, align = e1$align)
            right_part   <- edge_update(e2, leftnode  = e1$rightnode)
            c(edge_merge(e1, overlap_part), list(right_part))
        },
        EQUAL = {
            e1 <- edge_update(e1, leftnode = node_merge(e1$leftnode, e2$leftnode),
                                  rightnode = node_merge(e1$rightnode, e2$rightnode))
            if ((e1$type[1] == "Normal" && e2$type[1] != "Empty") ||
                (e1$type[1] != "Empty"  && e2$type[1] == "Normal")) {
                stop("Content Overlapping", call. = FALSE)
            }
            if (e1$type[1] == "Empty" || sum(c("HEADER", "FOOTER") %in% e2$type)) {
                e1 <- edge_update(e1, content = e2$content,
                                      type = e2$type,
                                      symbol = e2$symbol)
            }
            list(e1)
        },
        CONTAIN = {
            left_part    <- edge_update(e1, rightnode = e2$leftnode)
            overlap_part <- edge_update(e1, leftnode  = e2$leftnode,
                                            rightnode = e2$rightnode,
                                            align     = e2$align)
            right_part   <- edge_update(e1, leftnode  = e2$rightnode)
            c(list(left_part), edge_merge(overlap_part, e2), list(right_part))
        },
        CONTAIN_SAME_START = {
            overlap_part <- edge_update(e1, rightnode = e2$rightnode, align = e2$align)
            right_part   <- edge_update(e1, leftnode  = e2$rightnode)
            c(edge_merge(overlap_part, e2), list(right_part))
        }
    )
}

edge_relation <- function(e1, e2) {
    stopifnot(inherits(e1, "Edge") && inherits(e2, "Edge"))
    stopifnot(row_no(e1) == row_no(e2))
    range_relation(col_no(e1), col_no(e2))
}
edge_update <- function(e, ...) {
    attrs <- list(...)
    for (i in seq_along(attrs)) {
        e[[names(attrs)[i]]] <- attrs[[i]]
    }
    e
}

empty_row <- function(row_no, left_col_no = NULL, right_col_no = NULL) {
    stopifnot(is.null(left_col_no) == is.null(right_col_no))
    if (is.null(left_col_no) || is.null(right_col_no)) {
        return(structure(list(nodes = NULL,
                              edges = NULL,
                              n = 0,
                              row_no = row_no),
                              class = "Row"))
    }
    leftnode  <- Node(Coordinate(row_no, left_col_no),  SYMBOL$SIDE)
    rightnode <- Node(Coordinate(row_no, right_col_no), SYMBOL$SIDE)
    Row(Edge(leftnode, rightnode, " "))
}

format_column <- function(x, digits = 3L, ...) {
    stopifnot(is.atomic(x))
    if (is.character(x)) return(trimws(x))
    if (is.integer(x))   return(as.character(x))

    max_origin_digits <-
        strsplit(as.character(x), "\\.")  |>
        purrr::map_chr(\(r) purrr::pluck(r, 2, .default = "")) |>
        purrr::map_int(nchar) |>
        max()

    if (max_origin_digits == 0L) return(as.character(x))

    purrr::map_chr(x, format_one_num, digits = digits, ...) |>
    trimws()
}

format_one_num <- function(z, digits, nsmall = 3L,
                           width = NULL, na.replace = "", big.mark = ",") {
    stopifnot(is.numeric(z) && length(z) == 1L)
    if (is.null(digits)) stop("Must set digits", call. = FALSE)
    stopifnot(is.null(width) || width > digits)

    if (is.na(z))      return(na.replace)
    if (is.integer(z)) return(format(z, width = width, big.mark = big.mark))
    if (is.null(width)) width = digits + 3

    digits <- as.integer(digits)
    decbits <- if (abs(z) < 1) width - 0
               else            width - as.integer(log10(abs(round(z, digits = 0L)))) - 2

    if (decbits >= digits) {
        round(z, digits = digits) |>
        format(digits = digits, nsmall = digits, width = width)
    } else if (decbits > 0) {
        round(z, digits = digits) |>
        format(digits = decbits, nsmall = decbits,
               width = width, bit.mark = big.mark)
    } else {
        round(z, digits = 0) |>
        fomart(width = width, big.mark = big.mark)
    }
}

get_cells_from <- function(gtable) {
    rownum <- nrow(gtable)
    colnum <- ncol(gtable)
    cells <- purrr::map(seq_len(rownum), \(i) {
        purrr::map(seq_len(colnum), \(j) {
          Cell(gtable, i, j)
      })
    })
    do.call(c, cells)
}

#' @export
GridTable <- function(data, align = NULL,
                      header = NULL, footer = NULL, ...) {
    if (!inherits(data, "data.table")) data <- data.table::as.data.table(data)
    args <- list(...)
    align <- valid_align(data, align)

    data <- purrr::map_dfc(data, format_column) |> data.table::setDT()
    if (is.null(header) || header == 0L) {
        data <- rbind(as.list(colnames(data)), data)
        header <- 1L
    }

    if (is.null(footer)) footer <- Inf
    width <- purrr::map_int(data, cal_column_width)
    names(width) <- NULL
    height <- height_of(data)

    class(data) <- c("GridTable", class(data))
    data.table::setattr(data, "height", height)
    data.table::setattr(data, "width",  width)
    data.table::setattr(data, "align",  align)
    data.table::setattr(data, "header", header)
    data.table::setattr(data, "footer", footer)
    purrr::iwalk(args, \(x, y) data.table::setattr(data, y, x))
    data
}

height_of <- function(df, base = NULL) {
    h <- purrr::map_int(1:nrow(df), \(i) {
        purrr::map_int(
            df[i, ],
            \(x) if (is.na(x)) 0 else sum(gregexpr("\n", x)[[1]] > 0)
        ) |>
        max()
    }) + 1
    if (is.null(base)) return(h)
    else               return(ifelse(h > base, h, base))
}

ifthen <- function(x, y) {
    if (is.null(x)) y else x
}

integrate_edge_list <- function(edge_list) {
    old <- sort_edge_list(edge_list)
    if (length(edge_list) == 1) return(old)

    new <- list(shift(old))
    temp <- list(shift(old))
    while (length(old) > 0 || length(temp) > 0) {
        e <- if (length(temp) == 0)         shift(old)
             else if (length(old)  == 0)    shift(temp)
             else if (old[[1]] < temp[[1]]) shift(old)
             else                           shift(temp)
        m <- edge_merge(new[[length(new)]], e)
        if (e < m[[length(m)]]) {
            new <- c(new[-length(new)], m[-length(m)])
            temp <- sort_edge_list(c(m[length(m)], temp))
        } else {
            new <- c(new[-length(new)], m)
        }
    }
    new
}

is_overlaped <- function(x, y) {
    stopifnot(length(x) == 2 && length(y) == 2)
    get <- function(x, i) if (is.atomic(x))        x[i]     else x[[i]]
    min <- function(z)    if (get(z,1) < get(z,2)) get(z,1) else get(z,2)
    max <- function(z)    if (get(z,1) < get(z,2)) get(z,2) else get(z,1)
    !(min(x) > max(y) || min(y) > max(x))
}


#' @export
kable_to_grid <- function(kbl, ...) {
    stopifnot(inherits(kbl, "knitr_kable"))
    format <- attr(kbl, "format")
    stopifnot(format %in% c("simple", "pipe"))

    if (grepl("^Table:", kbl[1])) {
        caption <- kbl[1]
        kbl     <- kbl[-(1:2)]
    } else {
        caption <- NULL
    }
    sep <- " "
    if (format == "pipe") {
        sepline <- kbl[2]
        tbl   <- if (grepl("[^\\|\\s]", kbl[1], perl = TRUE)) kbl[-2] else kbl[-(1:2)]
        sep     <-  "|"
    } else if (grepl("[^\\-\\s]", kbl[length(kbl)], perl = TRUE)) {
        sepline <- kbl[2]
        tbl   <- kbl[-2]
    } else {
        sepline <- kbl[1]
        tbl   <- kbl[-c(1, length(kbl))]
    }

    start_end_points <- column_start_end_points(sepline, sep)
    stopifnot(!is.null(start_end_points))
    data <- purrr::map(tbl, \(line) {
        purrr::map_chr(start_end_points, \(x) substr_width(line, x[1], x[2]))
    })
    data <- do.call(rbind, data)

    args <- list(...)
    args$data <- data
    if (is.null(args$header)) args$header = 1L
    if (is.null(args$caption)) args$caption = caption
    do.call(GridTable, args)
}

last_node <- function(row) {
    if (row$n == 0) return(NULL)
    else            return(row$nodes[[row$n]])
}

#' @export
merge_cells <- function(tbl, i = NULL, j = NULL, cancel = NULL, ...) {
    old_merged_cell <- attr(tbl, "merged_cells")
    old_merged_cell_names <- names(old_merged_cell)

    if (is.null(i) != is.null(j)) {
        stop("Need to set both i and j", call. = FALSE)
    }

    if (is.null(i) && is.null(j) && is.null(cancel)) {
        purrr::iwalk(old_merged_cell, \(v, n) {
            cat("Name:", n, "\n")
            purrr::iwalk(v, ~ cat("\t", .y, ": ", toString(.x), "\n", sep = ""))
        })
        return(invisible(tbl))
    }

    merged_cell_name <- if (!is.null(i) && !is.null(j)) {
        paste(paste(unique(minmax(i)), collapse = ":"),
              paste(unique(minmax(j)), collapse = ":"),
              sep = ",")
    }

    if (!is.null(cancel)) {
        if (isTRUE(cancel) && !is.null(merged_cell_name)) {
            cancel <- merged_cell_name
        }
        if(!is.character(cancel)) {
            stop("Cancel needed to be TRUE, FALSE or names", call. = FALSE)
        }
        old_merged_cell_names <- setdiff(old_merged_cell_names, cancel)
        old_merged_cell <- old_merged_cell[old_merged_cell_names]
        data.table::setattr(tbl, "merged_cells", old_merged_cell)
        return(invisible(tbl))
    }

    merged_cell <- c(valid_merged_cell(i, j, tbl), list(...))
    purrr::walk(old_merged_cell, \(m1, m2) {
        if (is_overlaped(m1$rows, m2$rows) && is_overlaped(m1$cols, m2$cols)) {
            stop("There is overlap", call. = FALSE)
        }
    }, m2 = merged_cell)

    newattrs <- c(old_merged_cell, list(merged_cell))
    names(newattrs) <- c(old_merged_cell_names, merged_cell_name)
    data.table::setattr(tbl, "merged_cells", newattrs)
    return(invisible(tbl))
}

merge_cell_list <- function(cells) {
    cells <- cells[!purrr::map_lgl(cells, is.null)]
    purrr::walk(cells, \(c) stopifnot(inherits(c, "Cell")))

    longest <- max(purrr::map_int(cells, ~ attr(.x, "end")))
    new_rows <- purrr::map(seq_len(longest), \(i) {
        edges <- purrr::map(cells, \(c) {
            start <- attr(c, "start")
            end   <- attr(c, "end")
            if (i %in% start:end) c[[i - start + 1]] else NULL
        })
        edges <- edges[!purrr::map_lgl(edges, is.null)]
        do.call(Row, c(integrate_edge_list(edges), row_no = i))
    })
    do.call(Table, new_rows)
}

minmax <- function(x, na.rm = TRUE) {
    stopifnot(is.numeric(x))
    c(min(x, na.rm = na.rm), max(x, na.rm = na.rm))
}

Node <- function(coordinate, symbol = "+") {
    stopifnot(class(coordinate) == "Coordinate" && is.character(symbol))
    structure(list(coordinate = coordinate,
                   symbol = symbol),
              class = "Node")
}


node_merge <- function(n1, n2) {
    if (is.null(n1)) return(n2)
    if (is.null(n2)) return(n1)
    stopifnot(n1 == n2)
    if (n2$symbol == SYMBOL$VERTICE) return(n2)
    return(n1)
}

node_list_merge <- function(nodes1, nodes2) {
    stopifnot(do.call(row_no_allequal, c(nodes1, nodes2)))
    col_nos1 <- col_no(nodes1)
    col_nos2 <- col_no(nodes2)
    col_nos <- sort(unique(col_nos1, col2_nos1))
    purrr::map(col_nos, \(i) {
        node_merge(get_node_where(nodes1, i), get_node_where(nodes2, i))
    })
}


pandoc_wrap <- function(x) {
    nms <- names(x)
    names(x) <- NULL
    x <- purrr::imap_chr(strsplit(x, "[^\\]"), \(v, i) {
        if (nchar(v[length(v)]) %% 2 == 0) paste0(x[i], "\\")
        else                                  x[i]
    })
    names(x) <- nms
    x
}


parse_number_adjust <- function(num, x) {
    stopifnot(is.character(x))
    if (!is.atomic(x)) {
        x <- purrr::imap_chr(x, \(val,name) gettextf("%s%s", name, val))
    }
    x <- toupper(x)

    if (length(x) > 1) {
        return(parse_number_adjust(parse_number_adjust(num, x[1]), x[-1]))
    }
    elements <- str_match(x, "^([0-9a-zA-Z]+)([-+*/=]?)([0-9]+)")[[1]]
    if (length(elements) == 0 || is.na(elements[1])) {
        return(NULL)
    }
    else {
        elements <- elements[2:4]
    }

    index <- if (grepl("^[0-9]+$", elements[1])) {
        as.integer(elements[1])
    } else if (grepl("^[A-Za-z]+$", elements[1])) {
        which(LETTERS == strsplit(toupper(elements[1]), "")[[1]]) |> sum()
    }
    if (is.null(index)) return(NULL)

    stopifnot(index <= length(num))
    operand <- as.integer(elements[3])
    operator <- if (is.na(elements[2])) "=" else elements[2]
    switch(operator,
        `=` = { num[index] <- operand },
        `+` = { num[index] <- num[index] + operand },
        `-` = { num[index] <- num[index] - operand },
        `*` = { num[index] <- num[index] * operand },
        `/` = { num[index] <- as.integer(num[index] / operand) },
        default = stop("+-*/= or omit", call. = FALSE)
    )
    num
}

#' @export
print.GridTable <- function(gtable, drop_empty_line = TRUE, ...) {
  content <- toString(gtable, drop_empty_line = drop_empty_line, ...)
  cat(content, sep = "\n")
  invisible(content)
}

#' @export
row_no <- function(x) {
    switch(class(x),
        Coordinate = x[1],
        Node       = row_no(x$coordinate),
        Edge       = row_no(x$leftnode),
        Row        = x$row_no,
        default    = stop("wrong object")
    )
}

range_relation <- function(s1, s2, na.rm = TRUE) {
    l1 <- min(s1, na.rm = na.rm)
    l2 <- min(s2, na.rm = na.rm)
    r1 <- max(s1, na.rm = na.rm)
    r2 <- max(s2, na.rm = na.rm)

    if (r1 < l2) {
        return("LEFT")
    } else if (r1 == l2) {
        return("ADJACENT_LEFT")
    } else if (r1 > l2 & r1 < r2) {
        return(
            if      (l1 < l2)  "OVERLAP_LEFT"
            else if (l1 == l2) "IN_SAME_START"
            else               "In"
        )
    } else if (r1 == r2) {
        return(
            if      (l1 < l2)  "CONTAIN_SAME_END"
            else if (l1 == l2) "EQUAL"
            else               "IN_SAME_END"
        )
    } else {
        return(
            if      (l1 <  l2)           "CONTAIN"
            else if (l1 == l2)           "CONTAIN_SAME_START"
            else if (l1 > l2 && l1 < r2) "OVERLAP_RIGHT"
            else if (l1 == r2)           "ADJACENT_RIGHT"
            else                         "RIGHT"
        )
    }
}

row_no_allequal <- function(...) {
    args <- list(...)
    row_nos <- purrr::map(args, row_no) |> unlist() |> unique()
    length(row_nos) == 1L
}

Row <- function(..., row_no = NULL) {
    edges <- list(...)
    stopifnot(!(length(edges) == 0L && is.null(row_no)))
    if (length(edges) == 0L) return(empty_row(row_no))

    purrr::walk(edges, ~ stopifnot(class(.x) == "Edge"))
    edges <- local({
        edge_no <- purrr::map_int(edges, ~ .x$leftnode$coordinate[2])
        stopifnot(anyDuplicated(edge_no) == 0)
        purrr::map(seq_along(edges), ~ edges[[which(order(edge_no) == .x)]])
    })

    nodes <- vector("list", length(edges) + 1)
    for (i in seq_along(edges)) {
        if (i != 1) {
            stopifnot(edges[[i]]$leftnode == edges[[i-1]]$rightnode)
            edges[[i-1]]$rightnode <- node_merge(edges[[i]]$leftnode, edges[[i-1]]$rightnode)
            edges[[i]]$leftnode    <- edges[[i-1]]$rightnode
        }
        if (i == length(edges)) nodes[[i+1]] = edges[[i]]$rightnode
        nodes[[i]] = edges[[i]]$leftnode
    }

    structure(list(nodes = nodes,
                   edges = edges,
                   n = length(nodes),
                   row_no = row_no(edges[[1]])), class = "Row")
}


#' @export
set_attr <- function(tbl, attr = NULL, value = NULL, ...) {
    stopifnot(inherits(tbl, "GridTable"))
    purrr::iwalk(list(...), \(value, attr) set_attr(tbl, attr, value))
    if (is.null(attr)) return(invisible(tbl))

    switch(attr,
        align  = data.table::setattr(tbl, attr, valid_align(tbl, value)),
        height =,
        width  = {
            if (is.numeric(value)) {
                if (attr == "height") stopifnot(length(value) == nrow(tbl))
                if (attr == "width")  stopifnot(length(value) == ncol(tbl))
                data.table::setattr(tbl, attr, value)
            } else {
                data.table::setattr(tbl, attr,
                                    parse_number_adjust(attr(tbl, attr), value))
            }
        },
        data.table::setattr(tbl, attr, value)
    )
    invisible(tbl)
}

shift <- function (x, drop = TRUE) {
    if (length(x) == 0)
        return(NULL)
    outer_x <- as.character(substitute(x))
    shiftret <- if (isTRUE(drop) && is.list(x)) {
        x[[1]]
    }
    else {
        x[1, drop = drop]
    }
    assign(as.character(substitute(x)), x[-1], parent.frame())
    shiftret
}

sort_edge_list <- function(edge_list) {
    edge_list <- edge_list[!purrr::map_lgl(edge_list, is.null)]
    lcol_nos <- purrr::map_int(edge_list, \(e) col_no(e$leftnode))
    rcol_nos <- purrr::map_int(edge_list, \(e) col_no(e$rightnode))
    index <- lcol_nos * max(rcol_nos) + rcol_nos
    edge_list[order(index)]
}

str_width <- function(x) nchar(x, type = "width")
str_match <- function(x, pattern) regmatches(x, regexec(pattern, x))

substr_width <- function(x, start, end) {
    width     <- end - start + 1
    pre_width <- start - 1

    pre       <- substr(x, 1, start - 1)
    while (start >= 0 && str_width(pre) > pre_width) {
        start <- start - 1
        end   <- end - 1
        pre   <- substr(pre, 1, start - 1)
        if (str_width(pre) < pre_width) {
            stop("Start point cut character", call. = FALSE)
        }
    }

    subs <- substr(x, start, end)
    end  <- width
    while (end >= 0 && str_width(subs) > width) {
        end <- end - 1
        subs <- substr(subs, 1, end)
        if (str_width(subs) < width) {
            stop("End point cut character", call. = FALSE)
        }
    }

    subs
}

toInteger <- function(x) {
    stopifnot(!grepl(".", as.character(x + 0), fixed = TRUE))
    as.integer(x)
}

Table <- function(...) {
    rows    <- list(...)
    row_nos <- purrr::map_int(rows, "row_no")
    stopifnot(anyDuplicated(row_nos) == 0)

    length  <- max(row_nos)
    width   <- rows |>
               purrr::map_int(\(x) if (x$n == 0) 0 else col_no(last_node(x))) |>
               max()
    rows    <- purrr::map(seq_len(length), ~ {
        which_row <- which(row_nos == .x)
        if (length(which_row) != 0)   rows[[which_row]]
        else                          Row(row_no = .x)

    })
    structure(list(rows = rows, length = length, width = width),
              class = "Table")
}


toString.Edge <- function(edge) {
    symbol_l <- if (col_no(edge$leftnode) == 1L) edge$leftnode$symbol else ""
    symbol_r <- edge$rightnode$symbol
    width    <- col_no(edge$rightnode) - col_no(edge$leftnode) - 1
    if (edge$type[1] == "Normal") {
        content   <- trimws(edge$content)
        space_num <- width - 1 - str_width(content)
        content   <- paste0(" ", content, strrep(" ", space_num))
        return(gettextf("%s%s%s", symbol_l, content, symbol_r))
    }

    if ((!is.null(edge$align)) && "HEADER" %in% edge$type ) {
        align_symbol <- switch(edge$align,
            l = c(SYMBOL$ALIGN, ""),
            r = c("",           SYMBOL$ALIGN),
            c = c(SYMBOL$ALIGN, SYMBOL$ALIGN),
            default = stop("Align invalid", call. = FALSE)
        )
        edge_symbol_num <- width - sum(str_width(align_symbol))
        return(gettextf("%s%s%s%s%s", symbol_l, align_symbol[1],
                       strrep(edge$symbol, edge_symbol_num),
                       align_symbol[2], symbol_r))
    }
    return(gettextf("%s%s%s", symbol_l, strrep(edge$symbol, width), symbol_r))
}

toString.Row <- function(row) {
    paste0(purrr::map_chr(row$edges, toString.Edge), collapse = "")
}

#' @export
toString.GridTable <- function(gtable, ...) {
    rownum <- nrow(gtable)
    colnum <- ncol(gtable)
    args <- list(...)

    cells <- try(get_cells_from(gtable), silent = TRUE)

    if (inherits(cells, "try-error")) {
        msg <- geterrmessage()
        if (grepl("Adjust the width", msg, fixed = TRUE)) {
            message("Message: Adjusted table width attribute due to lengthy content")
        } else if (grepl("Adjust the height", msg, fixed = TRUE)) {
            message("Message: Adjusted table height attribute due to multiline content")
        }
        else {
            stop(message, call. = FALSE)
        }
        return(do.call(toString.GridTable, c(list(gtable), args)))
    }

    Table <- merge_cell_list(cells)
    content <- do.call(toString.Table, c(list(Table), args))
    if (!is.null(attr(gtable, "caption"))) {
        content <- c(attr(gtable, "caption"), "", content)
    }

    structure(content, class = "GridTable_output")
}

toString.Table <- function(tbl, drop_empty_line = TRUE, ...) {
    table_content <- purrr::map_chr(tbl$rows, ~ toString.Row(.x))
    if (isTRUE(drop_empty_line)) {
        table_content <- table_content[grepl("[^|\\s]", table_content, perl = TRUE)]
    }
    table_content
}


valid_align <- function(data, align = NULL) {
    if (is.null(align)) {
        align <-
            purrr::map_chr(data, \(x) {
                switch(class(x),
                    character = "l",
                    numeric = "r",
                    stop("Weird Column", call. = FALSE))
            })
    } else if (is.character(align)) {
        stopifnot(length(align) %in% c(1, length(data)))
        if (length(align) == 1)  {
            align <- strsplit(align, "")[[1]]
            if (length(align) == 1) align <- rep(align, length(data))
        }
        stopifnot(all(align %in% c("l", "r", "c")))
    } else {
        stop("align needed to be a character or character vector", call. = FALSE)
    }
    align
}

valid_merged_cell <- function(rows, cols, gridtable) {
    stopifnot(is.numeric(rows) && is.numeric(cols))
    stopifnot(inherits(gridtable, "GridTable"))

    merged_cell <-
        purrr::map(list(rows = rows, cols = cols), \(x) {
            mM <- minmax(x)
            if (length(x) > 2) stopifnot(all(sort(x) == mM[1]:mM[2]))
            mM
        })

    stopifnot(all(merged_cell$rows %in% 1:nrow(gridtable)))
    stopifnot(all(merged_cell$cols %in% 1:ncol(gridtable)))
    if (merged_cell$rows[1] < attr(gridtable, "header") &&
        merged_cell$rows[2] > attr(gridtable, "header")) {
        stop("Cannot Span header row", call. = FALSE)
    }

    merged_cell
}

# vim: set fdm=expr:
</file>

</files>
