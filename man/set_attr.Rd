% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/api.R
\name{set_attr}
\alias{set_attr}
\title{Adjust Render-State Attributes of a Grid Table}
\usage{
set_attr(tbl, attr = NULL, value = NULL, ...)
}
\arguments{
\item{tbl}{A \code{GridTable} object.}

\item{attr}{Name of the attribute to set (e.g. \code{"width"}, \code{"height"},
\code{"align"}). May be \code{NULL} when only \code{...} is used.}

\item{value}{New value, or an adjustment string/vector for \code{width}/\code{height}.}

\item{...}{Further \code{attr = value} pairs, each applied as an additional call.}
}
\value{
The (invisibly returned) \code{GridTable}, modified by reference.
}
\description{
Set or adjust an attribute of a \code{GridTable} by reference. Besides the generic
"set this attribute to this value" behaviour, the \code{width} and \code{height}
attributes accept a small adjustment DSL so individual columns/rows can be
grown or shrunk relative to their current value. Multiple attributes may be
set at once through \code{...}.
}
\details{
For \code{width} and \code{height}, \code{value} may be:
\itemize{
\item a numeric vector, used as the new attribute verbatim; or
\item a character adjustment string of the form \verb{<index><op><operand>}, parsed by
\code{parse_number_adjust()}. The \code{index} is either a number or a letter
(\code{A} = 1, \code{B} = 2, ...); \code{op} is one of \verb{+ - * / =} (omitting it means \code{=});
\code{operand} is an integer. For example \code{"2/2"} halves the second value,
\code{"b-3"} subtracts 3 from the second, and \code{"1+1"} increments the first. A
vector of such strings applies the adjustments in turn.
}

For \code{align}, \code{value} is validated by \code{valid_align()}. Any other attribute is
set verbatim.
}
\examples{
df  <- data.frame(a = c("x", "y"), b = c(1, 2))
tbl <- GridTable(df)
set_attr(tbl, "width", "1+3")   # widen column 1 by 3 display columns
set_attr(tbl, align = "c")      # centre every column (through `...`)
print(tbl)

}
\seealso{
\code{\link[=GridTable]{GridTable()}}
}
