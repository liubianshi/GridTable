% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/api.R
\name{merge_cells}
\alias{merge_cells}
\title{Register, Cancel, or List Merged Cell Regions}
\usage{
merge_cells(tbl, i = NULL, j = NULL, cancel = NULL, ...)
}
\arguments{
\item{tbl}{A \code{GridTable} object.}

\item{i}{Integer row indices of the region (must be supplied together with
\code{j}).}

\item{j}{Integer column indices of the region.}

\item{cancel}{\code{TRUE} to cancel the region described by \code{i}/\code{j}, or a character
vector of region names to cancel.}

\item{...}{Merge options stored with the region: \code{drop_content} (keep only
the top-left cell's content), \code{middle} (vertically centre the content),
\code{wrap} (pandoc line-wrap the content). See \code{MERGED_CELL_OPTION} for the
defaults.}
}
\value{
The (invisibly returned) \code{GridTable}, modified by reference.
}
\description{
Manage the rectangular merged-cell regions of a \code{GridTable}. Regions are
stored as a named-list attribute and consumed by the rendering engine, which
drops the internal borders so the region prints as a single cell. All forms
modify \code{tbl} by reference and return it invisibly.
}
\details{
Behaviour depends on which arguments are supplied:
\itemize{
\item \code{i} \strong{and} \code{j} given: register a new region spanning rows \code{i} and columns
\code{j}. The region may not straddle the header separator and may not overlap
an existing region.
\item \code{cancel} given: remove a region. Use \code{cancel = TRUE} together with \code{i}/\code{j}
to drop the region they describe, or pass the region name(s) as a character
vector.
\item nothing given: print the currently registered regions and return invisibly.
}
}
\examples{
df  <- data.frame(a = c("x", "y", "z"), b = c(1, 2, 3))
tbl <- GridTable(df)
merge_cells(tbl, i = 2:3, j = 1)   # merge column 1 across rows 2-3
print(tbl)

merge_cells(tbl)                   # list registered regions
merge_cells(tbl, i = 2:3, j = 1, cancel = TRUE)  # undo the merge

}
\seealso{
\code{\link[=GridTable]{GridTable()}}
}
