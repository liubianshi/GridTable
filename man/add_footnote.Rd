% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/api.R
\name{add_footnote}
\alias{add_footnote}
\title{Add a Footnote Below a Grid Table}
\usage{
add_footnote(tbl, note, ref = NULL, i = NULL, j = NULL)
}
\arguments{
\item{tbl}{A \code{GridTable} object.}

\item{note}{Character string: the footnote text. The reference marker is
added automatically from \code{ref}; do not include it here.}

\item{ref}{Optional character string: a reference symbol such as \code{"a"} or
\code{"*"}, rendered as a pandoc superscript \verb{^ref^}.}

\item{i, j}{Optional integer vectors selecting the cells to mark with the
\verb{^ref^} marker (every combination of \code{i} rows and \code{j} columns). Must be
supplied together, and require \code{ref}. Row indices follow the user-facing
convention: when the column names were prepended (\code{header = NULL}), row 1
is the header row.}
}
\value{
The (invisibly returned) \code{GridTable}, modified by reference.
}
\description{
Register a footnote on a \code{GridTable}. Footnotes are stored in the \code{notes}
attribute and rendered by \code{\link[=toString.GridTable]{toString.GridTable()}} as paragraphs \emph{below} the
table block, mirroring how \code{caption} is placed above it -- the table
geometry itself is untouched (no extra rows are inserted). When \code{ref} is
given, the note is prefixed with the pandoc superscript marker \verb{^ref^} and
the cells selected by \code{i}/\code{j} get the same marker appended to their content
(column widths grow automatically at render time). Modifies \code{tbl} by
reference and returns it invisibly.
}
\details{
When the table carries a \code{note_style} attribute (set it with
\code{GridTable(..., note_style = "Table Note")} or
\code{set_attr(tbl, note_style = "Table Note")}), the rendered notes are wrapped
in a pandoc fenced div \verb{::: \{custom-style="..."\}}. Pandoc applies custom
styles in docx, odt and ICML output (the style must exist in the reference
document); other writers ignore the attribute and the notes degrade to
plain paragraphs. Without \code{note_style} the notes are emitted as bare
paragraphs, keeping terminal output free of fence noise.

Cell markers are baked into the cell text at call time, while the note
itself lives in the \code{notes} attribute: clearing that attribute afterwards
(e.g. \code{set_attr(tbl, notes = NULL)}) does not remove markers already
placed in cells.
}
\examples{
df  <- data.frame(term = c("x1", "x2"), est = c(1.34, 2.1))
tbl <- GridTable(df)
add_footnote(tbl, "Standard errors in parentheses.", ref = "a", i = 1, j = 2)
add_footnote(tbl, "Source: simulated data.")
print(tbl)

# Wrap the notes in a custom-style div for docx output
set_attr(tbl, note_style = "Table Note")
print(tbl)

}
\seealso{
\code{\link[=GridTable]{GridTable()}}, \code{\link[=set_attr]{set_attr()}}
}
