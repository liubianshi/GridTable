% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/api.R
\name{GridTable}
\alias{GridTable}
\title{Render a Data Frame as a Pandoc Grid Table}
\usage{
GridTable(data, align = NULL, header = NULL, footer = NULL, ...)
}
\arguments{
\item{data}{A \code{data.frame} or \code{data.table} (anything coercible by
\code{\link[data.table:as.data.table]{data.table::as.data.table()}}). Numeric columns are formatted with
\code{format_one_num()}; character columns are trimmed.}

\item{align}{Column alignment. Either \code{NULL} (default: left for character
columns, right for numeric), a single character such as \code{"l"}, \code{"r"} or
\code{"c"} recycled to every column, or a string/vector of per-column codes such
as \code{"lcr"}.}

\item{header}{Number of leading rows that form the table header: the header
separator line (drawn with \code{=}) is placed below row \code{header}. When \code{NULL}
or \code{0}, the column names are prepended as the first row and become the
header (\code{header = 1}); user-facing row indices then start at 2 for the
first data row.}

\item{footer}{Row index of the first table-foot row: a \code{=} separator line
is drawn above that row, so the rows from \code{footer} to the bottom print as
the table foot (pandoc's table foot, the analogue of HTML \verb{<tfoot>} --
e.g. a totals row). This is \emph{not} a footnote mechanism. Defaults to \code{Inf}
(no table foot).}

\item{...}{Additional attributes stored on the returned object via
\code{\link[data.table:setattr]{data.table::setattr()}}. The most useful are \code{caption}, a string rendered
above the table, and \code{note_style}, a paragraph style name wrapping the
footnote block (see \code{\link[=add_footnote]{add_footnote()}}).}
}
\value{
An object of class \code{GridTable} (a \code{data.table} with render-state
attributes). Use \code{\link[=print.GridTable]{print.GridTable()}} or \code{\link[=toString.GridTable]{toString.GridTable()}} to render
it.
}
\description{
Build a \code{GridTable} object from a \code{data.frame}/\code{data.table}. The result is a
\code{data.table} carrying all render state (column widths, row heights,
alignment, header/footer positions) as attributes; print it with
\code{\link[=print.GridTable]{print.GridTable()}} to obtain the plain-text grid table. The layout engine is
CJK-width aware (it measures everything with \code{nchar(type = "width")}), so
columns containing Chinese or other wide characters stay aligned.
}
\examples{
df <- data.frame(name = c("Alice", "Bob"), score = c(1.5, 22))
print(GridTable(df))

# Per-column alignment plus a caption (passed through `...`)
print(GridTable(df, align = "lc", caption = "Table: scores"))

# CJK-width awareness keeps wide characters aligned
print(GridTable(data.frame(a = c("中文", "x"), b = c(1.5, 22))))

}
\seealso{
\code{\link[=merge_cells]{merge_cells()}} to merge cells, \code{\link[=set_attr]{set_attr()}} to tweak widths and
heights, \code{\link[=kable_to_grid]{kable_to_grid()}} to build from a \code{knitr::kable}.
}
