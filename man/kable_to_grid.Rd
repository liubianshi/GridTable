% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/api.R
\name{kable_to_grid}
\alias{kable_to_grid}
\title{Build a Grid Table from a 'knitr' kable}
\usage{
kable_to_grid(kbl, ...)
}
\arguments{
\item{kbl}{A \code{knitr_kable} object produced with \code{format = "pipe"} or
\code{format = "simple"}.}

\item{...}{Further arguments forwarded to \code{\link[=GridTable]{GridTable()}} (e.g. \code{align},
\code{footer}, \code{caption}). \code{header} defaults to \code{1} and \code{caption} defaults to
the kable's own \verb{Table:} line when not supplied.}
}
\value{
An object of class \code{GridTable}.
}
\description{
Reverse-parse a \code{knitr::kable()} (in \code{"pipe"} or \code{"simple"} format) back into
a \code{GridTable}. The kable separator line is used to recover the column
boundaries (via \code{column_start_end_points()}); each data line is then sliced
by display width (via \code{substr_width()}) so the original column contents are
recovered as character cells. A leading \verb{Table:} caption line, if present, is
carried over.
}
\examples{
if (requireNamespace("knitr", quietly = TRUE)) {
  df  <- data.frame(name = c("Alice", "Bob"), score = c(1, 22))
  kbl <- knitr::kable(df, format = "pipe")
  print(kable_to_grid(kbl))
}

}
\seealso{
\code{\link[=GridTable]{GridTable()}}
}
